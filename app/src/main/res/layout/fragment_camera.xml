<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Camera Preview (will be either CameraView for regular camera or ARSceneView for AR) -->
    <FrameLayout
        android:id="@+id/camera_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.camera.view.PreviewView
            android:id="@+id/camera_preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </FrameLayout>

    <!-- Stream Information Overlay -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/stream_info_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardBackgroundColor="#88000000"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <TextView
                android:id="@+id/tv_stream_url"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@android:color/white"
                android:textSize="12sp"
                tools:text="Stream URL: http://192.168.1.100:8080" />

            <TextView
                android:id="@+id/tv_stream_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@android:color/white"
                android:textSize="12sp"
                tools:text="Status: Streaming" />

            <TextView
                android:id="@+id/tv_stream_clients"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@android:color/white"
                android:textSize="12sp"
                tools:text="Connected Clients: 1" />

            <TextView
                android:id="@+id/tv_stream_resolution"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@android:color/white"
                android:textSize="12sp"
                tools:text="Resolution: 1920x1080" />

            <TextView
                android:id="@+id/tv_stream_fps"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@android:color/white"
                android:textSize="12sp"
                tools:text="FPS: 30" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- AR Status Indicator -->
    <TextView
        android:id="@+id/tv_ar_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:background="@drawable/rounded_status_background"
        android:padding="8dp"
        android:text="@string/status_ar_ready"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:visibility="visible" />

    <!-- Camera Controls Panel -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/camera_controls_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardBackgroundColor="#88000000"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- Camera control buttons row -->
            <LinearLayout
                android:id="@+id/camera_controls_row"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="horizontal"
                app:layout_constraintTop_toTopOf="parent">

                <ImageButton
                    android:id="@+id/btn_switch_camera"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_margin="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/camera_switch"
                    android:padding="8dp"
                    app:srcCompat="@drawable/ic_switch_camera"
                    app:tint="@android:color/white" />

                <ImageButton
                    android:id="@+id/btn_flash"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_margin="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/camera_flash"
                    android:padding="8dp"
                    app:srcCompat="@drawable/ic_flash_off"
                    app:tint="@android:color/white" />

                <ImageButton
                    android:id="@+id/btn_capture"
                    android:layout_width="64dp"
                    android:layout_height="64dp"
                    android:layout_margin="8dp"
                    android:background="@drawable/circle_button_background"
                    android:contentDescription="@string/camera_capture"
                    android:padding="16dp"
                    app:srcCompat="@drawable/ic_camera_capture"
                    app:tint="@android:color/white" />

                <ImageButton
                    android:id="@+id/btn_focus"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_margin="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/camera_focus"
                    android:padding="8dp"
                    app:srcCompat="@drawable/ic_focus"
                    app:tint="@android:color/white" />

                <ImageButton
                    android:id="@+id/btn_settings"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_margin="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/settings_tab"
                    android:padding="8dp"
                    app:srcCompat="@drawable/ic_settings"
                    app:tint="@android:color/white" />
            </LinearLayout>

            <!-- Zoom slider -->
            <LinearLayout
                android:id="@+id/zoom_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:orientation="horizontal"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@+id/camera_controls_row"
                tools:visibility="visible">

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:contentDescription="@string/camera_zoom"
                    app:srcCompat="@drawable/ic_zoom_out"
                    app:tint="@android:color/white" />

                <com.google.android.material.slider.Slider
                    android:id="@+id/zoom_slider"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:valueFrom="1.0"
                    android:valueTo="10.0"
                    app:trackColorActive="@android:color/white"
                    app:trackColorInactive="#66FFFFFF" />

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:contentDescription="@string/camera_zoom"
                    app:srcCompat="@drawable/ic_zoom_in"
                    app:tint="@android:color/white" />
            </LinearLayout>

            <!-- Exposure slider -->
            <LinearLayout
                android:id="@+id/exposure_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:orientation="horizontal"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@+id/zoom_container"
                tools:visibility="visible">

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:contentDescription="@string/camera_exposure"
                    app:srcCompat="@drawable/ic_exposure_minus"
                    app:tint="@android:color/white" />

                <com.google.android.material.slider.Slider
                    android:id="@+id/exposure_slider"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:valueFrom="-2.0"
                    android:valueTo="2.0"
                    app:trackColorActive="@android:color/white"
                    app:trackColorInactive="#66FFFFFF" />

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:contentDescription="@string/camera_exposure"
                    app:srcCompat="@drawable/ic_exposure_plus"
                    app:tint="@android:color/white" />
            </LinearLayout>

            <!-- Streaming controls row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:gravity="center"
                android:orientation="horizontal"
                app:layout_constraintTop_toBottomOf="@+id/exposure_container">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_toggle_stream"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/action_stop"
                    android:textColor="@android:color/white"
                    app:backgroundTint="#E53935"
                    app:cornerRadius="24dp"
                    app:icon="@drawable/ic_stop_streaming"
                    app:iconTint="@android:color/white" />

                <Space
                    android:layout_width="16dp"
                    android:layout_height="wrap_content" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_toggle_ar"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/ar_enable"
                    android:textColor="@android:color/white"
                    app:backgroundTint="#2196F3"
                    app:cornerRadius="24dp"
                    app:icon="@drawable/ic_ar_mode"
                    app:iconTint="@android:color/white" />
            </LinearLayout>
        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>